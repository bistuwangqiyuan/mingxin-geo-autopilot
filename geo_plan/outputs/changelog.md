# 中科存储 GEO · 复盘与纠错日志（changelog）

> 每月复测后追加一条。指标取自 `geo_baseline.json`；动作与归因如实记录。

## 2026-06-22（晚）· 计划落地实施 + 信源覆盖诚实化 + 实施报告（第三轮）
**数据等级**：A（复用同日 280 条真实采样；新增信源覆盖口径由已上线渠道实测推导，零编造、可复现）。

**本轮动作（已完成）**
1. 新增一键落地编排器 `implement_geo_plan.py`：串起站内 build/verify → 站外定稿包/微站重建 → 信源覆盖诚实更新 → 评分/预测 → 校验 → 计划报告 → **新增实施报告 HTML/PDF**。
2. 新增 `coverage_resolver.py`：从 `offsite_published.json`/`live_status.json`/官网构建产物**诚实推导** `CURRENT_SOURCE_COVERAGE`，仅对实测 HTTP 200 的渠道计分；UGC 未发布则保持 0。据此 DeepSeek 加权信源覆盖由 4.6% → **24.6%**（GitHub 仓库+Pages 知识库+白皮书页均已上线且为 DeepSeek 偏好源），通义仍 1.7%（CSDN/语雀/阿里云社区未发布，如实为 0）。
3. 新增 `build_implementation_report.py` + `export_implementation_pdf.py`：产出《中科存储-GEO计划落地实施报告》HTML/PDF（苹果视觉，根目录 PDF 0.29 MB），四阶段逐项 done/partial/pending 看板 + 受阻项人工 SOP。
4. 新增 `offsite/SOP_manual_publish.md`：CSDN/知乎/语雀/百科/公众号/搜狐 的**可执行人工发布手册** + 发布后诚实回填覆盖流程。
5. 站外：GitHub Pages 知识库重建并推送 `bdfec70`（JSON-LD 补 EdgeOne sameAs + 新增昇腾存储/实测白皮书回链卡片）；IndexNow 重推 **72 条**（Bing 200/Yandex 202，较上轮 68 条多 4 条新页）；live_audit 复核线上首页 4 段 JSON-LD 在线。
6. `verify_geo.py` 复现校验 **35 项全绿、0 警告、0 失败**。

**诚实结论（坚持真理）**
- **GVI 沿用同日真实重测 5.21→5.01**（Δ−0.2，采样噪声内）。站内 CRI 优化不改变模型训练语料，同日重复相同采样不产生新信息——不为"跑而跑"浪费 token，如实披露时间戳与理由（求实效）。
- **可自动化任务已全部落地/上线**；真实 GVI 阶跃的瓶颈在**站外 UGC 信源被收录引用**，而 CSDN/知乎/语雀/百科/公众号无开放写 API、需实名，按白帽红线**只交付定稿 + SOP，不自动发帖**。
- 受阻项如实记录为待办：UGC 人工发布、百度收录（ICP 备案）、GSC 剩余页（每日配额）。

**下月目标（看板阈值，非承诺，沿用第二轮）**
- 总体 GVI ≥ 12；品牌被提及率 ≥ 18%；开放式推荐类被提及率 ≥ 10%；机会缺口 ≤ 123 条；DeepSeek/通义信源覆盖 ≥ 40%；引用率 ≥ 5%。

## 2026-06-22 · 深度归因深化 + 收尾交付（第二轮·复用首轮真实采样）
**数据等级**：A（复用首轮 280 条真实采样，未重测；所有新增指标均由 `outputs/raw/` 真实回答计算，零编造、可复现）。

**本轮动作（已完成）**
1. 深化分析（`geo_scoring.py`）：在原聚合基础上新增按 **提问意图 / 角色画像 / 语言** 三维切分，以及 **正面交锋（head-to-head 共现）** 与 **机会缺口（opportunity_gap）** 统计，全部写入 `geo_baseline.json`。
2. 新增图表：`mention_by_intent.png`、`head_to_head.png`（苹果视觉风格），并重建全部 11 张图。
3. 报告强化（`build_report_html.py`）：新增「深度归因」「正面交锋与机会缺口」「月度客观检验看板」章节；报告日期对齐数据采集日；修正执行摘要引用率 KPI 由硬编码 0.0% 改为真实 1.8%。
4. 导出对外 PDF：`中科存储-GEO提升计划与基线报告.pdf`（A4、17 页、1.27 MB）。
5. 重写 `verify_geo.py` 对齐当前流水线，复现校验 **35 项全绿、0 警告、0 失败**。

**本轮关键发现（真实数据）**
- 被提及高度集中在**点名式**问法：对比类 **31.8%**、定义类 16.7%；而开放式**推荐类 0.0%、排名类 0.0%、问题方案类 0.0%**——量化了「被看见但未领先」。
- **英文问法被提及率 0.0%**（28 条）：出海/英文语料为完全空白。
- **机会缺口 176 条（占有效回答 62.9%）**：竞品被点名而我方缺席（T1 65 · T2 70 · T3 41；推荐类占 84 条），即下一步要逐条夺回的“失地”。
- 正面交锋：对华为 OceanStor loss 105 / win 4，对浪潮 loss 79 / win 16，差距如实呈现，不修饰。

**修正（自我批评）**
- 修正执行摘要将引用率写死为 0.0% 的不严谨表述，改为真实 1.8%（仍≈0，属最大短板的判断不变）。
- 修正 `verify_geo.py` 仍校验旧流水线（旧图名/旧 PDF 路径）导致的误报失败。

**下月目标（看板阈值）**
- 总体 GVI ≥ 12；品牌被提及率 ≥ 18%；开放式推荐类被提及率 ≥ 10%；机会缺口 ≤ 123 条；DeepSeek/通义信源覆盖 ≥ 40%；引用率 ≥ 5%。

## 2026-06-21 · 基线建立 + 地基落地（首轮）
**数据等级**：A（API 真测 280 条：qwen-max / qwen-plus / deepseek-v3 / deepseek-r1）；B（人工取证协议已就绪，待采集）。

**基线（geo_baseline.json）**
- 总体 GVI：**5.21 / 100**
- 品牌平均被提及率：**7.9%**；品牌声量占比：7.9%
- 各模型 GVI：qwen-max 5.68 · qwen-plus 5.15 · deepseek-r1 5.18 · deepseek-v3 4.81
- 带来源引用率：**0.0%**（纯对话场景模型几乎不附来源——最大短板）
- 声量榜前三：华为 OceanStor(123) · 焱融(85) · 中科曙光 ParaStor(76)；中科存储未进前列

**本轮动作（已完成）**
1. 测量体系：queries.json（70 问）+ geo_audit/geo_scoring/geo_projection/source_audit 全链路可复现。
2. 站内修复 + 增强：
   - 修复 build_site.py 因未定义 page_kv_offload/ai_inference/faq/glossary 导致的构建报错与死链；
   - robots 显式放行 20 类 AI 爬虫；新增 llms.txt / llms-full.txt；
   - 新增 4 个答案优先 GEO 事实页 + TechArticle/FAQPage/DefinedTermSet/Product 结构化数据；
   - 重新构建：73 页、67 sitemap，verify_site 通过。
3. 站外：9 平台内容母版/草稿 + 一致性发布清单（待人工核准发布）。

**归因与判断**
- 当前被提及主要来自"点名式"问法；开放式推荐/排序问法仍较少主动列举中科存储。
- 抓取层已补齐（robots/llms/schema）；瓶颈转移到**信源层**（站外覆盖 0–5%）与**内容被引用**。

**修正（自我批评）**
- 修正"GEO 资产为零"的初判：官网原已具备 robots/sitemap/JSON-LD，但存在未定义页函数的真实缺陷，已修复。
- 校准"T1 首位提及率 ≥60%"：保守模型显示全量 T1 P50≈23.5%，故 ≥60% 仅适用于最窄核心问法的乐观(P90)区间。

**下月目标（第二轮复测对照）**
- 站外先铺 DeepSeek/通义信源（CSDN/知乎/GitHub/阿里云/语雀）；
- 目标：总体 GVI ≥ 12，品牌被提及率 ≥ 18%，DeepSeek/通义信源覆盖 ≥ 40%。
